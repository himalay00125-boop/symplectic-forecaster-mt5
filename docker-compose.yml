version: '3.8'

services:
  forecaster:
    build: .
    container_name: symplectic_forecaster
    ports:
      - "8080:8080"
    environment:
      - IS_DOCKER=true
      - MT5_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
